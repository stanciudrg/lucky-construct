import "./contact.css";

const header = document.querySelector("header");
header.classList.remove("invisible");

const navItem = document.querySelector("#contact");
navItem.classList.add("selected");
